//commonjs, every file is a module
//Modules - encapsulated code (only share the minimum)

const name = require("./name4")
const message = require("./utils5")
const data = require("./alternative-flavor6")
require("./mind-grenade7")

console.log(data.items[0])
console.log(data.marriedPerson)
message(name.knoll)


